[
    {
        "path": "no-mans-sky-trainer/requirements.txt",
        "content": "pymem>=1.0.0\nrequests>=2.28.0\ncolorama>=0.4.6\npsutil>=5.9.0"
    },
    {
        "path": "no-mans-sky-trainer/src/__init__.py",
        "content": ""
    },
    {
        "path": "no-mans-sky-trainer/src/memory_reader.py",
        "content": "import pymem\nimport pymem.process\nimport struct\nfrom typing import Optional, Tuple, Any\n\n\nclass MemoryReader:\n    \"\"\"\n    Handles reading and writing to the No Man's Sky process memory.\n    Uses pymem for process attachment and memory manipulation.\n    \"\"\"\n\n    def __init__(self, process_name: str = \"NMS.exe\"):\n        self.process_name = process_name\n        self.pm: Optional[pymem.Pymem] = None\n        self.base_address: Optional[int] = None\n\n    def attach(self) -> bool:\n        \"\"\"\n        Attach to the No Man's Sky process.\n        Returns True if successful, False otherwise.\n        \"\"\"\n        try:\n            self.pm = pymem.Pymem(self.process_name)\n            self.base_address = pymem.process.module_from_name(\n                self.pm.process_handle, self.process_name\n            ).lpBaseOfDll\n            return True\n        except pymem.exception.ProcessNotFound:\n            return False\n        except Exception as e:\n            print(f\"[ERROR] Failed to attach: {e}\")\n            return False\n\n    def read_int(self, address: int) -> Optional[int]:\n        \"\"\"Read a 4-byte integer from the given address.\"\"\"\n        try:\n            return self.pm.read_int(address)\n        except Exception:\n            return None\n\n    def write_int(self, address: int, value: int) -> bool:\n        \"\"\"Write a 4-byte integer to the given address.\"\"\"\n        try:\n            self.pm.write_int(address, value)\n            return True\n        except Exception:\n            return False\n\n    def read_float(self, address: int) -> Optional[float]:\n        \"\"\"Read a 4-byte float from the given address.\"\"\"\n        try:\n            return self.pm.read_float(address)\n        except Exception:\n            return None\n\n    def write_float(self, address: int, value: float) -> bool:\n        \"\"\"Write a 4-byte float to the given address.\"\"\"\n        try:\n            self.pm.write_float(address, value)\n            return True\n        except Exception:\n            return False\n\n    def read_bytes(self, address: int, size: int) -> Optional[bytes]:\n        \"\"\"Read a sequence of bytes from memory.\"\"\"\n        try:\n            return self.pm.read_bytes(address, size)\n        except Exception:\n            return None\n\n    def write_bytes(self, address: int, data: bytes) -> bool:\n        \"\"\"Write bytes to memory.\"\"\"\n        try:\n            self.pm.write_bytes(address, data)\n            return True\n        except Exception:\n            return False\n\n    def get_pointer_address(\n        self, base: int, offsets: Tuple[int, ...]\n    ) -> Optional[int]:\n        \"\"\"\n        Resolve a multi-level pointer to its final address.\n        Example: base + offset1 -> deref -> + offset2 -> deref -> ...\n        \"\"\"\n        addr = base\n        try:\n            for offset in offsets:\n                addr = self.pm.read_int(addr) + offset\n            return addr\n        except Exception:\n            return None\n\n    def close(self) -> None:\n        \"\"\"Cleanly detach from the process.\"\"\"\n        if self.pm:\n            self.pm.close_process()\n            self.pm = None\n"
    },
    {
        "path": "no-mans-sky-trainer/src/offsets.py",
        "content": "from typing import Dict, Tuple\n\n\nclass Offsets:\n    \"\"\"\n    Central repository for memory offsets used in No Man's Sky Trainer.\n    These offsets are version-specific and may need updating after patches.\n    \"\"\"\n\n    # Base pointer offsets for player stats (example structure)\n    PLAYER_BASE_OFFSETS: Tuple[int, ...] = (0x12345678, 0x90, 0x4A, 0x10)\n\n    # Currency (Units)\n    UNITS_OFFSET: int = 0x20\n\n    # Nanite Clusters\n    NANITES_OFFSET: int = 0x24\n\n    # Quicksilver\n    QUICKSILVER_OFFSET: int = 0x28\n\n    # Health (Shield + Core)\n    HEALTH_OFFSET: int = 0x30\n    MAX_HEALTH_OFFSET: int = 0x34\n\n    # Hazard Protection\n    HAZARD_PROTECTION_OFFSET: int = 0x38\n    MAX_HAZARD_PROTECTION_OFFSET: int = 0x3C\n\n    # Jetpack Energy\n    JETPACK_ENERGY_OFFSET: int = 0x40\n    MAX_JETPACK_ENERGY_OFFSET: int = 0x44\n\n    # Inventory slot count (current ship)\n    INVENTORY_SLOTS_OFFSET: int = 0x50\n\n    # Coordinates (X, Y, Z floats)\n    COORDINATES_X_OFFSET: int = 0x60\n    COORDINATES_Y_OFFSET: int = 0x64\n    COORDINATES_Z_OFFSET: int = 0x68\n\n    # Game speed modifier (float)\n    GAME_SPEED_OFFSET: int = 0x100\n\n    # Experience / standing (example)\n    STANDING_OFFSET: int = 0x70\n\n    @classmethod\n    def get_player_stat_offsets(cls) -> Dict[str, int]:\n        \"\"\"Return a dictionary of all player stat offsets for easy access.\"\"\"\n        return {\n            \"units\": cls.UNITS_OFFSET,\n            \"nanites\": cls.NANITES_OFFSET,\n            \"quicksilver\": cls.QUICKSILVER_OFFSET,\n            \"health\": cls.HEALTH_OFFSET,\n            \"max_health\": cls.MAX_HEALTH_OFFSET,\n            \"hazard_protection\": cls.HAZARD_PROTECTION_OFFSET,\n            \"max_hazard_protection\": cls.MAX_HAZARD_PROTECTION_OFFSET,\n            \"jetpack_energy\": cls.JETPACK_ENERGY_OFFSET,\n            \"max_jetpack_energy\": cls.MAX_JETPACK_ENERGY_OFFSET,\n            \"inventory_slots\": cls.INVENTORY_SLOTS_OFFSET,\n            \"pos_x\": cls.COORDINATES_X_OFFSET,\n            \"pos_y\": cls.COORDINATES_Y_OFFSET,\n            \"pos_z\": cls.COORDINATES_Z_OFFSET,\n            \"game_speed\": cls.GAME_SPEED_OFFSET,\n            \"standing\": cls.STANDING_OFFSET,\n        }\n"
    },
    {
        "path": "no-mans-sky-trainer/src/trainer.py",
        "content": "import time\nimport sys\nfrom typing import Dict, Any, Optional\nfrom colorama import Fore, Style, init as colorama_init\n\nfrom src.memory_reader import MemoryReader\nfrom src.offsets import Offsets\n\n\nclass NoMansSkyTrainer:\n    \"\"\"\n    Main trainer class for No Man's Sky.\n    Provides methods to modify player stats, teleport, and more.\n    \"\"\"\n\n    def __init__(self):\n        colorama_init(autoreset=True)\n        self.memory = MemoryReader()\n        self.attached = False\n        self.running = False\n        self.player_base: Optional[int] = None\n\n    def attach_to_game(self) -> bool:\n        \"\"\"Attempt to attach to the No Man's Sky process.\"\"\"\n        print(f\"{Fore.YELLOW}[*] Attaching to No Man's Sky...{Style.RESET_ALL}\")\n        if self.memory.attach():\n            self.attached = True\n            print(f\"{Fore.GREEN}[+] Attached successfully!{Style.RESET_ALL}\")\n            return True\n        else:\n            print(f\"{Fore.RED}[-] Failed to attach. Is NMS running?{Style.RESET_ALL}\")\n            return False\n\n    def get_player_base(self) -> Optional[int]:\n        \"\"\"Resolve the player base address from offsets.\"\"\"\n        if not self.attached or not self.memory.base_address:\n            return None\n        base = self.memory.get_pointer_address(\n            self.memory.base_address, Offsets.PLAYER_BASE_OFFSETS\n        )\n        self.player_base = base\n        return base\n\n    def set_units(self, amount: int) -> bool:\n        \"\"\"Set player units to a specific amount.\"\"\"\n        base = self.get_player_base()\n        if base is None:\n            return False\n        return self.memory.write_int(base + Offsets.UNITS_OFFSET, amount)\n\n    def set_nanites(self, amount: int) -> bool:\n        \"\"\"Set nanite clusters.\"\"\"\n        base = self.get_player_base()\n        if base is None:\n            return False\n        return self.memory.write_int(base + Offsets.NANITES_OFFSET, amount)\n\n    def set_quicksilver(self, amount: int) -> bool:\n        \"\"\"Set quicksilver currency.\"\"\"\n        base = self.get_player_base()\n        if base is None:\n            return False\n        return self.memory.write_int(base + Offsets.QUICKSILVER_OFFSET, amount)\n\n    def max_health(self) -> bool:\n        \"\"\"Set current health to maximum.\"\"\"\n        base = self.get_player_base()\n        if base is None:\n            return False\n        max_hp = self.memory.read_int(base + Offsets.MAX_HEALTH_OFFSET)\n        if max_hp is None:\n            return False\n        return self.memory.write_int(base + Offsets.HEALTH_OFFSET, max_hp)\n\n    def infinite_jetpack(self, enable: bool) -> bool:\n        \"\"\"Toggle infinite jetpack by setting energy to max.\"\"\"\n        base = self.get_player_base()\n        if base is None:\n            return False\n        if enable:\n            max_energy = self.memory.read_int(base + Offsets.MAX_JETPACK_ENERGY_OFFSET)\n            if max_energy is None:\n                return False\n            return self.memory.write_int(base + Offsets.JETPACK_ENERGY_OFFSET, max_energy)\n        else:\n            # Just leave as-is; no actual disable logic\n            return True\n\n    def teleport_to_coordinates(self, x: float, y: float, z: float) -> bool:\n        \"\"\"Teleport player to specific coordinates.\"\"\"\n        base = self.get_player_base()\n        if base is None:\n            return False\n        success = True\n        success &= self.memory.write_float(base + Offsets.COORDINATES_X_OFFSET, x)\n        success &= self.memory.write_float(base + Offsets.COORDINATES_Y_OFFSET, y)\n        success &= self.memory.write_float(base + Offsets.COORDINATES_Z_OFFSET, z)\n        return success\n\n    def set_game_speed(self, speed: float) -> bool:\n        \"\"\"Set game speed multiplier (e.g., 2.0 for double speed).\"\"\"\n        base = self.get_player_base()\n        if base is None:\n            return False\n        return self.memory.write_float(base + Offsets.GAME_SPEED_OFFSET, speed)\n\n    def run_interactive(self) -> None:\n        \"\"\"Run an interactive console menu for the trainer.\"\"\"\n        if not self.attach_to_game():\n            return\n        self.running = True\n        print(f\"{Fore.CYAN}=== No Man's Sky Trainer ==={Style.RESET_ALL}\")\n        print(\"Commands:\")\n        print(\"  1 - Set Units to 999999999\")\n        print(\"  2 - Set Nanites to 99999\")\n        print(\"  3 - Set Quicksilver to 99999\")\n        print(\"  4 - Max Health\")\n        print(\"  5 - Infinite Jetpack (toggle)\")\n        print(\"  6 - Teleport to (0, 50, 0)\")\n        print(\"  7 - Set Game Speed to 2.0\")\n        print(\"  0 - Exit\")\n\n        while self.running:\n            try:\n                cmd = input(f\"{Fore.YELLOW}> {Style.RESET_ALL}\").strip()\n                if cmd == \"1\":\n                    if self.set_units(999999999):\n                        print(f\"{Fore.GREEN}[+] Units set!{Style.RESET_ALL}\")\n                    else:\n                        print(f\"{Fore.RED}[-] Failed.{Style.RESET_ALL}\")\n                elif cmd == \"2\":\n                    if self.set_nanites(99999):\n                        print(f\"{Fore.GREEN}[+] Nanites set!{Style.RESET_ALL}\")\n                    else:\n                        print(f\"{Fore.RED}[-] Failed.{Style.RESET_ALL}\")\n                elif cmd == \"3\":\n                    if self.set_quicksilver(99999):\n                        print(f\"{Fore.GREEN}[+] Quicksilver set!{Style.RESET_ALL}\")\n                    else:\n                        print(f\"{Fore.RED}[-] Failed.{Style.RESET_ALL}\")\n                elif cmd == \"4\":\n                    if self.max_health():\n                        print(f\"{Fore.GREEN}[+] Health maxed!{Style.RESET_ALL}\")\n                    else:\n                        print(f\"{Fore.RED}[-] Failed.{Style.RESET_ALL}\")\n                elif cmd == \"5\":\n                    if self.infinite_jetpack(True):\n                        print(f\"{Fore.GREEN}[+] Jetpack set to max!{Style.RESET_ALL}\")\n                    else:\n                        print(f\"{Fore.RED}[-] Failed.{Style.RESET_ALL}\")\n                elif cmd == \"6\":\n                    if self.teleport_to_coordinates(0.0, 50.0, 0.0):\n                        print(f\"{Fore.GREEN}[+] Teleported!{Style.RESET_ALL}\")\n                    else:\n                        print(f\"{Fore.RED}[-] Failed.{Style.RESET_ALL}\")\n                elif cmd == \"7\":\n                    if self.set_game_speed(2.0):\n                        print(f\"{Fore.GREEN}[+] Game speed set to 2.0!{Style.RESET_ALL}\")\n                    else:\n                        print(f\"{Fore.RED}[-] Failed.{Style.RESET_ALL}\")\n                elif cmd == \"0\":\n                    self.running = False\n                    print(f\"{Fore.YELLOW}[*] Exiting...{Style.RESET_ALL}\")\n                else:\n                    print(f\"{Fore.RED}Unknown command.{Style.RESET_ALL}\")\n            except KeyboardInterrupt:\n                self.running = False\n                print(f\"\\n{Fore.YELLOW}[*] Exiting...{Style.RESET_ALL}\")\n            except Exception as e:\n                print(f\"{Fore.RED}[ERROR] {e}{Style.RESET_ALL}\")\n\n        self.memory.close()\n\n\ndef main():\n    \"\"\"Entry point for the trainer.\"\"\"\n    trainer = NoMansSkyTrainer()\n    trainer.run_interactive()\n\n\nif __name__ == \"__main__\":\n    main()\n"
    },
    {
        "path": "no-mans-sky-trainer/tests/__init__.py",
        "content": ""
    },
    {
        "path": "no-mans-sky-trainer/tests/test_offsets.py",
        "content": "import unittest\nfrom src.offsets import Offsets\n\n\nclass TestOffsets(unittest.TestCase):\n    \"\"\"Test suite for Offsets class.\"\"\"\n\n    def test_player_base_offsets_not_empty(self):\n        \"\"\"Ensure PLAYER_BASE_OFFSETS is a non-empty tuple.\"\"\"\n        self.assertTrue(len(Offsets.PLAYER_BASE_OFFSETS) > 0)\n        for offset in Offsets.PLAYER_BASE_OFFSETS:\n            self.assertIsInstance(offset, int)\n\n    def test_get_player_stat_offsets_returns_dict(self):\n        \"\"\"Ensure get_player_stat_offsets returns a dictionary with expected keys.\"\"\"\n
